#!/bin/sh
xrandr --output DP-1 --auto --scale 1.0x1.0 --mode 1920x1080 --rotate left --output eDP-1 --off

postrun() { # clean up
  { conky | dzen2 -h "50" -p -dock -ta l -fn "Fira Mono-10" & }
}

postrun
