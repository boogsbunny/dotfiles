;;--------------------------------------------------------------------
;; zig
;;--------------------------------------------------------------------

(add-to-list 'major-mode-remap-alist '(zig-mode . zig-ts-mode))

(provide 'init-zig)
